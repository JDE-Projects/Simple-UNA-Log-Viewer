name: CI
on:
  pull_request:
  push:
    branches: [main]
jobs:
  ci:
    uses: JDE-Projects/Build-Tools/.github/workflows/ci-windows.yml@5ecb1d806d02d7dbf6af2b53c932b7e7cb64a7ed # Build-Tools main (ruff, import check, pytest)
    permissions:
      contents: read
    with:
      entry_script: "simple_una_log_viewer.py"
