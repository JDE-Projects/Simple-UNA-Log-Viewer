name: CI
on:
  pull_request:
  push:
    branches: [main]
jobs:
  ci:
    uses: JDE-Projects/Build-Tools/.github/workflows/ci-windows.yml@01090b68a33074fda7fcd97d088cb29aa9718f61 # Build-Tools main (ruff, import check, pytest)
    permissions:
      contents: read
    with:
      entry_script: "simple_una_log_viewer.py"
