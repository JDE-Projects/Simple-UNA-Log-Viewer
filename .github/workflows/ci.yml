name: CI
on:
  pull_request:
  push:
    branches: [main]
jobs:
  ci:
    uses: JDE-Projects/Build-Tools/.github/workflows/ci-windows.yml@3736646d6f665a2b8671e4cf8c5c2973a93c4d14 # Build-Tools main (ruff, import check, pytest)
    permissions:
      contents: read
    with:
      entry_script: "simple_una_log_viewer.py"
