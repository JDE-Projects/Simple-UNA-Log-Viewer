# Empty on purpose: its presence at the repo root makes pytest add the repo
# root to sys.path, so tests can import the app module without relying on the
# working directory pytest happened to be launched from.
