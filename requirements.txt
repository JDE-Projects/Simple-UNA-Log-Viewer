# Pinned build dependencies for Simple UNA Log Viewer.
# Versions match the JDE pilot's green CI build (Python 3.14).
# Dependabot opens review-PRs for these == deps when newer releases appear.
pywebview==6.2.1
PySide6==6.11.1
qtpy==2.4.3
pyinstaller==6.22.0
truststore==0.10.4
